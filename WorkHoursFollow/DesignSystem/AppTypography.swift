import SwiftUI

enum AppTypography {
    static let title = Font.system(.largeTitle, design: .serif, weight: .semibold)
    static let metric = Font.system(.largeTitle, design: .serif, weight: .semibold)
}
